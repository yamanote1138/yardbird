services:
  yardbird-dev:
    build:
      context: .
      dockerfile: Dockerfile
      target: builder
    container_name: yardbird-dev
    restart: unless-stopped
    ports:
      - ${DEV_PORT:-5173}:5173
      - ${DCCEX_WS_PORT:-2561}:2561
    environment:
      - NODE_ENV=development
      - DCCEX_HOST=${DCCEX_HOST:-}
      - DCCEX_PORT=${DCCEX_PORT:-2560}
      - DCCEX_WS_PORT=${DCCEX_WS_PORT:-2561}
      - VITE_YB_JMRI_HOST=${YB_JMRI_HOST:-}
      - VITE_YB_JMRI_PORT=${YB_JMRI_PORT:-}
      - VITE_YB_JMRI_SECURE=${YB_JMRI_SECURE:-}
      - VITE_YB_MOCK=${YB_MOCK:-}
      - VITE_YB_DEBUG=${YB_DEBUG:-}
      - VITE_YB_DCCEX_ENABLED=${YB_DCCEX_ENABLED:-}
      - VITE_YB_DCCEX_HOST=${YB_DCCEX_HOST:-}
      - VITE_YB_DCCEX_PORT=${YB_DCCEX_PORT:-}
      - VITE_YB_DCCEX_PWM_FREQ=${YB_DCCEX_PWM_FREQ:-}
      - VITE_YB_HA_ENABLED=${YB_HA_ENABLED:-}
      - VITE_YB_HA_URL=${YB_HA_URL:-}
      - VITE_YB_HA_TOKEN=${YB_HA_TOKEN:-}
      - VITE_YB_HA_AREA=${YB_HA_AREA:-}
    volumes:
      - ./src:/usr/src/app/src
      - ./public:/usr/src/app/public
      - ./index.html:/usr/src/app/index.html
      - ./vite.config.ts:/usr/src/app/vite.config.ts
      - ./proxy:/usr/src/app/proxy
    command: >
      sh -c "
        if [ -n \"$$DCCEX_HOST\" ]; then
          node proxy/dccex-ws-proxy.mjs &
        fi;
        npm run dev -- --host 0.0.0.0
      "
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:5173"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s
